[{'print(f': 'Executing {len(statements)'}, {'sql': 'stmt'}, {'executed': "except Exception as e:\n            # Some statements may fail if objects already exist (IF NOT EXISTS handles this)\n            if 'already exists' in str(e).lower() or 'duplicate' in str(e).lower():\n                print(f", 'i}': 'object already exists (continuing)', 'else': 'print(f', 'failed': {'print(f': 'QL: {stmt[:100]'}, ')\n                # Continue anyway - some statements may be optional\n\n    print("\n✅ Migration completed!")\n    print(': 'Next steps:', 'print(': '.', 'gateway.js': 'python patch_gateway_vendor_inventory_routes.py', 'Test': "curl -H 'Authorization: Bearer TOKEN' http://127.0.0.1:8080/api/vendors", '__main__': 'main()'}]