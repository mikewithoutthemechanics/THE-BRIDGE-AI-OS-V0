[{'status': 'healthy', 'timestamp': '...'}, {'task': 'test task', 'duration': 1}, {'task_id': '...', 'status': 'queued'}, {'status': 'healthy', 'persona': 'oracle', 'cycles': 'N', 'http': 'localhost:9091/health\n\n# Should see corrections applied\n```\n\n### 7. Grafana Dashboard (Production)\n\n```bash\n# If running full stack with Prometheus+Grafana:\n# Access http://localhost:3000\n# Login: admin / bridge-ai-admin\n# Navigate to: Dashboards → OVERSEER — Sovereign Authority Layer\n\n# Verify panels show:\n# - Cycles/sec\n# - Corrections/sec\n# - Violations by type\n# - System health gauge\n# - Current persona\n```\n\n### 8. Integration Test Suite\n\n```bash\ncd overseer\npython -m pytest tests/test_overseer.py -v\n\n# Expected: All tests pass\n```\n\n### 9. Load Test (Optional)\n\n```bash\n# Generate multiple requests\nfor i in {1..10', '{"bad":"state"}': 'var/lib/bridge/state.json'}, {'task":"load test $i"}': 'dev/null\ndone\n\n# Check Overseer handled the load\ncurl -s http://localhost:9091/health | jq .cycles\n```\n\n## Expected Results\n\n| Test | Expected Result |\n|------|----------------|\n| Container status | Both services running', 'unhealthy': "check if API is responding:\ndocker exec bridge-overseer curl -f http://localhost:9091/health\n\n# Restart if needed\ndocker restart bridge-overseer\n```\n\n### Backend not responding\n```bash\n# Check logs\ndocker logs bridge-backend\n\n# Verify port binding\ndocker port bridge-backend\n\n# Restart\ndocker restart bridge-backend\n```\n\n### Metrics not appearing in Grafana\n```bash\n# Check Prometheus scrape config\ncurl http://localhost:9090/api/v1/targets | jq '.data.activeTargets[] | select(.labels.service==", 'overseer': '\n\n# If targets are down, check Prometheus logs\ndocker-compose logs prometheus\n\n# Reload Prometheus config\ndocker kill -s HUP $(docker ps -q --filter name=prometheus)\n```\n\n## Success Criteria\n\n✅ All containers running and healthy  \n✅ Backend API responds to health checks  \n✅ Task submission creates tasks  \n✅ Overseer cycles execute without errors  \n✅ Metrics endpoint returns Prometheus data  \n✅ Persona switching works  \n✅ Audit log contains causal chain  \n✅ Tests pass (optional)  \n\n---\n\n## Quick One-Line Tests\n\n```bash\n# All-in-one test\ndocker-compose up -d && \n  docker ps | grep bridge && \n  curl -s http://localhost:8000/health && \n  curl -s http://localhost:9091/health && \n  echo "✓ System operational"\n```'}]