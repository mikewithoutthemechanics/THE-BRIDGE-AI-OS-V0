server {
    listen 80;
    server_name _;

    location = /api/health {
        default_type application/json;
        return 200 '{"service":"business","status":"healthy","timestamp":"${date_iso_8601}"}';
    }

    location / {
        try_files $uri $uri/ =404;
    }
}