═══════════════════════════════════════════════════════════════════════════════
             SUPADASH 8-DAY STREAMING: 3-LAPTOP QUICK START
═══════════════════════════════════════════════════════════════════════════════

TIMING: This is CRITICAL. Follow exact order and timing.

THREE LAPTOPS:
  L1 (Laptop 1):  Claude Code (6 Claude agents)         - Port 9000
  L2 (Laptop 2):  Kimi Code (7 Specialist agents)       - Port 9001
  L3 (Laptop 3):  VSCode (4+ Minimax M2.5 instances)    - Port 9002

═══════════════════════════════════════════════════════════════════════════════
PREP (Before Day 1 Hour 0)
═══════════════════════════════════════════════════════════════════════════════

On ALL THREE laptops:
──────────────────────

  cd /c/aoe-unified-final
  mkdir -p shared LOGS STANDUPS AGENTS
  git checkout feature/supadash-consolidation || git checkout -b feature/supadash-consolidation
  git status    # Should be clean

  # Network test (L1 only):
  ping laptop2
  ssh laptop2 "echo OK"
  ping laptop3
  ssh laptop3 "echo OK"

═══════════════════════════════════════════════════════════════════════════════
DAY 1, HOUR 0: BOOTSTRAP
═══════════════════════════════════════════════════════════════════════════════

On L1 (Terminal 1):
────────────────────

  cd /c/aoe-unified-final
  ./agents/bootstrap-day1.sh

  # Wait ~30-60 seconds for output:
  # ✓ BOOTSTRAP COMPLETE
  # ✓ L1 (Port 9000): 6 Claude agents ready
  # L2 and L3 deploy in parallel (see below)


On L2 (Terminal 2, from L1):
────────────────────────────

  # Deploy L2 (waits for L1)
  ssh laptop2 "bash -s" < /c/aoe-unified-final/setup-laptop2-unattended.sh &

  # Setup takes ~2-3 minutes
  # SSH session stays open


On L3 (Terminal 3, from L1):
────────────────────────────

  # Deploy L3 (waits for L1)
  # Default: 4 Minimax instances
  ssh laptop3 "bash -s" < /c/aoe-unified-final/setup-laptop3-unattended.sh &

  # Or custom instance count:
  # MINIMAX_INSTANCES=6 ssh laptop3 "bash -s" < setup-laptop3-unattended.sh &

  # Setup takes ~2-3 minutes
  # SSH session stays open


Monitor (Terminal 4):
─────────────────────

  watch -n 5 'git log --oneline | head -10'

  # Or check individual status:
  # curl http://localhost:9000/api/status | jq '.agents | length'  # Should be 6
  # curl http://localhost:9001/api/status | jq '.agents | length'  # Should be 7
  # curl http://localhost:9002/api/status | jq '.instances'        # Should be 4+

═══════════════════════════════════════════════════════════════════════════════
DAY 1, HOUR 4: PUBLISH CONTRACTS (4 hours after bootstrap)
═══════════════════════════════════════════════════════════════════════════════

On L1:
──────

  cd /c/aoe-unified-final
  ./agents/publish-contracts-day1.sh

  # Wait for output:
  # ✓ CONTRACTS PUBLISHED
  # ✓ 5 contracts now live

  # L2 and L3 auto-pull within 5 minutes
  # All agents can NOW start real work

═══════════════════════════════════════════════════════════════════════════════
DAYS 2-8: STREAMING EXECUTION (AUTOMATIC)
═══════════════════════════════════════════════════════════════════════════════

What to Do: NOTHING. Everything runs automatically.

What Happens:
─────────────

  Days 2-5:    L1: Code generation (gateway, dashboards, data, auth, tests)
               L2: Verification + validation (tests run continuously)
               L3: Code consolidation + optimization (in parallel)

               All three run in PARALLEL, NO BLOCKING

  Day 5:       ✓ All 43 features verified (GATE PASSES)
               ✓ All core systems stable
               ✓ L3 optimizations applied

  Days 6-7:    L1: Edge case handling, documentation
               L2: Performance baseline, security validation
               L3: Final performance tuning

               7-day load test clean

  Day 8:       Dry-run cutover
               Production cutover (if dry-run passes)

Daily Check (Optional, 09:00 UTC):
──────────────────────────────────

  # From L1, view status:
  curl http://localhost:9000/api/status | jq '.agents'  # L1: 6
  curl http://localhost:9001/api/status | jq '.agents'  # L2: 7
  curl http://localhost:9002/api/status | jq '.instances' # L3: 4+

  # Check for conflicts or optimizations:
  curl http://localhost:9001/api/conflicts | jq
  curl http://localhost:9002/api/optimization | jq

  # View logs:
  tail -20 LOGS/LAPTOP_1.log
  ssh laptop2 "tail -20 /c/aoe-unified-final/LOGS/LAPTOP_2.log"
  ssh laptop3 "tail -20 /c/aoe-unified-final/LOGS/LAPTOP_3.log"

═══════════════════════════════════════════════════════════════════════════════
MONITORING URLS
═══════════════════════════════════════════════════════════════════════════════

L1 Status:         http://localhost:9000/api/status
L1 Agents:         http://localhost:9000/api/agents (6 Claude agents)

L2 Status:         http://localhost:9001/api/status  (or http://laptop2:9001/)
L2 Agents:         http://localhost:9001/api/agents (7 Specialist agents)
L2 Conflicts:      http://localhost:9001/api/conflicts

L3 Status:         http://localhost:9002/api/status  (or http://laptop3:9002/)
L3 Agents:         http://localhost:9002/api/agents (N Minimax instances)
L3 Optimizations:  http://localhost:9002/api/optimization/<component>

Feature Matrix:    cat SUPADASH_FEATURE_MATRIX.md | grep "✓"

═══════════════════════════════════════════════════════════════════════════════
CRITICAL GATES (Must All Pass Before Production)
═══════════════════════════════════════════════════════════════════════════════

Gate                   | By Day | Check
───────────────────────┼────────┼──────────────────────────────────────────
Gateway Stable         | 2      | curl http://localhost:9000/api/status
Features All ✓         | 5      | grep "✓" SUPADASH_FEATURE_MATRIX.md
Data Zero Loss         | 4      | curl http://localhost:9001/api/verification/data
L3 Optimizations Done  | 5      | curl http://localhost:9002/api/status
Load Test 7 Days       | 8      | tail LOGS/LAPTOP_2.log | grep load-test

═══════════════════════════════════════════════════════════════════════════════
PARALLEL WORK BREAKDOWN
═══════════════════════════════════════════════════════════════════════════════

L1 (Code Generation)          L2 (Verification)           L3 (Optimization)
──────────────────────────────┼─────────────────────────────┼──────────────────
Days 2-5:                     Days 2-5:                    Days 2-5:
- Gateway (Agent 2A)          - Stress test gateway       - Consolidate gateway code
- Dashboard (Agent 3A)        - UI optimization           - Optimize database queries
- Migrations (Agent 4A)       - Query optimization        - Performance tuning
- Auth (Agent 5A)             - Load test (24/7)          - Consolidate auth code
- Tests (Agent 6A)            - Conflict detection        - Parallel sub-agents:
                                                            * Code consolidation (SA1)
                                                            * Query optimization (SA2)
                                                            * Performance tuning (SA3)

All three run in PARALLEL. No blocking. Results merge automatically.

═══════════════════════════════════════════════════════════════════════════════
TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

Issue: L2 or L3 stuck waiting
──────────────────────────────
  Check: Is L1 running?
  Fix: curl http://localhost:9000/health
       If error: go back and run bootstrap on L1 first

Issue: No contracts showing (after Hour 4)
───────────────────────────────────────────
  Check: Did you run publish-contracts-day1.sh?
  Fix: ./agents/publish-contracts-day1.sh

Issue: L3 not showing optimizations
─────────────────────────────────────
  Check: curl http://localhost:9002/api/status
  Fix: Give it 5-10 minutes, then check again
       ssh laptop3 "tail -50 LOGS/LAPTOP_3.log"

Issue: Orchestrator crashed (L1, L2, or L3)
─────────────────────────────────────────────
  Check: cat LOGS/LAPTOP_*.log | tail -50
  Fix: Kill old process, restart
       pkill -f "orchestrator"
       node agents/laptop*-streaming-orchestrator.js --port 900* &

═══════════════════════════════════════════════════════════════════════════════
SUCCESS CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

Day 1 Hour 0:
  ✓ L1 orchestrator running (port 9000)
  ✓ 6 Claude agents initialized
  ✓ L2 orchestrator running (port 9001)
  ✓ 7 specialist agents initialized
  ✓ L3 orchestrator running (port 9002)
  ✓ 4+ Minimax instances initialized (12+ sub-agents)

Day 1 Hour 4:
  ✓ 5 contracts published
  ✓ All agents can see contracts
  ✓ No blockers reported
  ✓ L3 starting consolidation + optimization

Days 2-5:
  ✓ Code commits flowing (git log shows activity)
  ✓ Tests running (test count increasing)
  ✓ No critical conflicts
  ✓ L3 optimization results pushed to shared/

Day 5:
  ✓ GATE PASSED: All 43 features verified
  ✓ Load test 4 days clean
  ✓ Systems stable
  ✓ L3 optimizations applied

Day 8:
  ✓ Dry-run cutover successful
  ✓ Production cutover executed
  ✓ L2 monitoring active
  ✓ L3 optimization results saved

═══════════════════════════════════════════════════════════════════════════════
FINAL TIMELINE
═══════════════════════════════════════════════════════════════════════════════

  Hour 0:   Bootstrap L1 → Deploy L2 & L3 in parallel
  Hour 4:   Publish contracts
  Days 2-5: Parallel development (L1) + verification (L2) + optimization (L3)
  Day 5:    Feature gate passes, ready for hardening
  Days 6-7: Hardening + 7-day load test clean
  Day 8:    Production cutover

TOTAL: 8 DAYS with THREE LAPTOPS in parallel

vs

  16 days sequential (original)
  18 days single-laptop parallel (L1 only)
  12-14 days two-laptop parallel (L1 + L2 only)

═══════════════════════════════════════════════════════════════════════════════
THAT'S IT. YOU'RE READY WITH 3 LAPTOPS.
═══════════════════════════════════════════════════════════════════════════════

When you're ready:

1. Terminal 1: ./agents/bootstrap-day1.sh
2. Terminal 2: ssh laptop2 "bash -s" < setup-laptop2-unattended.sh &
3. Terminal 3: ssh laptop3 "bash -s" < setup-laptop3-unattended.sh &
4. Terminal 4: watch -n 5 'git log --oneline | head -10'
5. Wait 4 hours
6. Run: ./agents/publish-contracts-day1.sh
7. Do nothing for 8 days
8. Cutover to production

Three laptops working in perfect parallel. No coordination issues. No blocking.
All automation, zero manual intervention.

🚀
